describe("myPod", function () {
    it('Should toggle and close div when the button is pressed', function () {
        expect(false).toBe(false);
    });
    
    it('Should toggle and open div when the button is pressed', function () {
        expect(true).toBe(true);
    });
});